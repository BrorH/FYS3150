#include "Matrix.h"
#include "common.h"

int main() {
  // int exp = atoi(argv[1]);
  Matrix A("dat.dat");
  // Matrix A(2, 2);
  // A.set(0, 0, 2);
  // A.set(1, 1, 1);
  // Matrix B(2, 1);
  // B.set(0, 0, 5);
  // B.set(1, 0, 7);
  //
  // A.print();
  // cout << endl;
  // B.print();
  // cout << endl;
  //
  // (A * B).print();

  return 0;
}
